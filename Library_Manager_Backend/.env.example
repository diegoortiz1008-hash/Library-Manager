# Si no se crea un .env, docker-compose.yml usa estos mismos valores por defecto.

APP_PORT=8080

# Puerto en el host para PostgreSQL
DB_PORT=5432

# Credenciales de la base de datos
POSTGRES_DB=biblioteca_db
POSTGRES_USER=prueba
POSTGRES_PASSWORD=prueba
